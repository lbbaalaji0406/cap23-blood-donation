{
  "rules": {
    "users": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'Admin'",
      "$uid": {
        ".read": "auth != null && (auth.uid == $uid || root.child('users').child(auth.uid).child('role').val() == 'Admin')",
        ".write": "auth != null && (\n          root.child('users').child(auth.uid).child('role').val() == 'Admin'\n          || (\n            auth.uid == $uid && (\n              (!data.exists() && newData.child('role').val() == 'User' && !newData.child('campId').exists())\n              || (data.exists() && newData.child('role').val() == data.child('role').val() && newData.child('campId').val() == data.child('campId').val())\n            )\n          )\n        )"
      }
    },

    "masters": {
      ".read": "auth != null",
      ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'Admin'"
    },

    "transactions": {
      "donation_request": {
        "$id": {
          ".read": "auth != null && (\n            root.child('users').child(auth.uid).child('role').val() == 'Admin'\n            || root.child('users').child(auth.uid).child('campId').val() == data.child('campId').val()\n          )",
          ".write": "auth != null && (\n            root.child('users').child(auth.uid).child('role').val() == 'Admin'\n            || (\n              root.child('users').child(auth.uid).child('role').val() == 'Manager'\n              && root.child('users').child(auth.uid).child('campId').val() == data.child('campId').val()\n              && newData.child('status').val() != 'Closed'\n              && newData.child('status').val() != 'Unfulfilled'\n            )\n          ) && newData.child('recipientName').exists()\n            && newData.child('blood_groupId').exists()\n            && newData.child('campId').exists()\n            && (!data.exists() || newData.child('campId').val() == data.child('campId').val())"
        }
      }
    },

    "donor_history": {
      "$donorUid": {
        ".read": "auth != null && (auth.uid == $donorUid || root.child('users').child(auth.uid).child('role').val() != 'User')",
        ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() != 'User'"
      }
    },

    "comments": {
      "$transactionId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "attachments": {
      "$transactionId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },

    "auditLogs": {
      ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'Admin'",
      ".write": "auth != null"
    },

    "settings": {
      ".read": "auth != null",
      ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() == 'Admin'"
    }
  }
}
